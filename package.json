{
  "name": "hubtel-ps",
  "version": "1.0.0",
  "description": "hubtel progammable services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prepublish": "npm run build",
    "publish": "git push --tags && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.5",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "loki": "^0.28.1",
    "lokijs": "^1.5.12"
  }
}
